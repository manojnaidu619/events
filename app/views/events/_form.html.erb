<%= form_for @event, html: { multipart: true } do |f| %>   <!-- @event from events_contoller.rb -->
 <%= f.label :name %><br>
 <%= f.text_field :name%><br>
 <%= f.label :description %><br>
 <%= f.text_area :description %><br>
 <%= f.file_field :image %><br><br>           <!-- File field for uploading Images -->
 <%= f.submit 'Save' %>
<% end %>
<%= link_to 'Home', root_path %>
